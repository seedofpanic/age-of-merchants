<?php defined('SYSPATH') OR die('No direct script access.');

class Admin_Controller extends Controller_Template {
	private $isAdmin;
	
	public function before()
	{
		parent::before();
		$this->isAdmin = $user->has('role', 'admin');
		if (!$this->isAdmin)
		{
			$this->request->
		}
	}
	
}
