include inc/header
.ui.container
   .ui.segment
      .ui.grid
         .four.wide.column
            .ui.segment.inverted
               .ui.inverted.relaxed.divided.list
                  a.item(ng-click='mc.open(region)' ng-repeat='region in mc.regions') {{region.name}}
         .twelve.wide.column
            h1 {{mc.selected.name}}
            include inc/tile_map

